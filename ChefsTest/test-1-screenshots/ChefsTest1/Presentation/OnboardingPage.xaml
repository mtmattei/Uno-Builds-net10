<Page x:Class="ChefsTest1.Presentation.OnboardingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:p="using:ChefsTest1.Presentation"
      Background="{ThemeResource BackgroundBrush}">
  <utu:SafeArea Insets="VisibleBounds" Mode="Padding">
    <Grid Padding="0,0,0,24">
      <Grid.RowDefinitions>
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <FlipView x:Name="HeroFlip"
                Grid.Row="0"
                ItemsSource="{x:Bind ViewModel.Frames, Mode=OneWay}"
                SelectedIndex="{x:Bind ViewModel.SelectedIndex, Mode=TwoWay}">
        <FlipView.ItemTemplate>
          <DataTemplate x:DataType="p:OnboardingFrame">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>
              <Image Source="{x:Bind Image}"
                     Stretch="UniformToFill"
                     Grid.Row="0" />
              <StackPanel Grid.Row="1" Spacing="8" Padding="24,24,24,8" HorizontalAlignment="Center">
                <Image Source="ms-appx:///Assets/Images/chefsappsignature_light.svg"
                       Width="120" Height="56" Stretch="Uniform" />
                <TextBlock Text="{x:Bind Title}"
                           HorizontalAlignment="Center"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="{ThemeResource OnSurfaceBrush}" />
                <TextBlock Text="{x:Bind Body}"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           Foreground="{ThemeResource OnSurfaceVariantBrush}" />
              </StackPanel>
            </Grid>
          </DataTemplate>
        </FlipView.ItemTemplate>
      </FlipView>

      <StackPanel Grid.Row="1" Spacing="16" Padding="24,8">
        <PipsPager NumberOfPages="{x:Bind ViewModel.Frames.Count, Mode=OneWay}"
                   SelectedPageIndex="{x:Bind ViewModel.SelectedIndex, Mode=TwoWay}"
                   HorizontalAlignment="Center" />
        <Grid ColumnSpacing="12">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>
          <Button Content="Previous"
                  Grid.Column="0"
                  HorizontalAlignment="Stretch"
                  Style="{StaticResource OutlinedButtonStyle}"
                  Command="{x:Bind ViewModel.PreviousCommand}" />
          <Button Content="Next"
                  Grid.Column="1"
                  HorizontalAlignment="Stretch"
                  Style="{StaticResource FilledButtonStyle}"
                  Command="{x:Bind ViewModel.NextCommand}" />
        </Grid>
        <Button Content="Skip"
                HorizontalAlignment="Center"
                Style="{StaticResource TextButtonStyle}"
                Command="{x:Bind ViewModel.SkipCommand}" />
      </StackPanel>
    </Grid>
  </utu:SafeArea>
</Page>
