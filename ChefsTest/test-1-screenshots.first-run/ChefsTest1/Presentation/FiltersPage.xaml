<Page x:Class="ChefsTest1.Presentation.FiltersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:ChefsTest1.Presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      Background="{ThemeResource ScreenBackgroundBrush}"
      mc:Ignorable="d">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Padding="20,16">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Button Grid.Column="0" Background="Transparent" BorderThickness="0" Margin="0,0,12,0" Command="{x:Bind ViewModel.CloseCommand}">
        <FontIcon Glyph="&#xE711;" FontSize="20" />
      </Button>
      <TextBlock Grid.Column="1" Text="Filters" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
    </Grid>

    <ScrollViewer Grid.Row="1">
      <StackPanel Padding="20,0" Spacing="22">
        <StackPanel Spacing="10">
          <TextBlock Text="Recipe Categories" FontWeight="SemiBold" FontSize="15" />
          <StackPanel Orientation="Horizontal" Spacing="10">
            <Button Style="{StaticResource ChipActiveButtonStyle}" Content="Popular" Command="{x:Bind ViewModel.SetCategoryCommand}" CommandParameter="Popular" />
            <Button Style="{StaticResource ChipButtonStyle}" Content="Trending" Command="{x:Bind ViewModel.SetCategoryCommand}" CommandParameter="Trending" />
            <Button Style="{StaticResource ChipButtonStyle}" Content="Recent" Command="{x:Bind ViewModel.SetCategoryCommand}" CommandParameter="Recent" />
          </StackPanel>
        </StackPanel>
        <StackPanel Spacing="10">
          <TextBlock Text="Cooking Time" FontWeight="SemiBold" FontSize="15" />
          <StackPanel Orientation="Horizontal" Spacing="10">
            <Button Style="{StaticResource ChipActiveButtonStyle}" Content="15 min" Command="{x:Bind ViewModel.SetTimeCommand}" CommandParameter="15 min" />
            <Button Style="{StaticResource ChipButtonStyle}" Content="30 min" Command="{x:Bind ViewModel.SetTimeCommand}" CommandParameter="30 min" />
            <Button Style="{StaticResource ChipButtonStyle}" Content="60 min" Command="{x:Bind ViewModel.SetTimeCommand}" CommandParameter="60 min" />
          </StackPanel>
        </StackPanel>
        <StackPanel Spacing="10">
          <TextBlock Text="Skill Level" FontWeight="SemiBold" FontSize="15" />
          <StackPanel Orientation="Horizontal" Spacing="10">
            <Button Style="{StaticResource ChipActiveButtonStyle}" Content="Beginner" Command="{x:Bind ViewModel.SetSkillCommand}" CommandParameter="Beginner" />
            <Button Style="{StaticResource ChipButtonStyle}" Content="Intermediate" Command="{x:Bind ViewModel.SetSkillCommand}" CommandParameter="Intermediate" />
            <Button Style="{StaticResource ChipButtonStyle}" Content="Advance" Command="{x:Bind ViewModel.SetSkillCommand}" CommandParameter="Advance" />
          </StackPanel>
        </StackPanel>
      </StackPanel>
    </ScrollViewer>

    <Grid Grid.Row="2" Padding="20" ColumnSpacing="12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Button Grid.Column="0" Style="{StaticResource OutlineButtonStyle}" Content="Reset" Command="{x:Bind ViewModel.ResetCommand}" />
      <Button Grid.Column="1" Style="{StaticResource PrimaryCtaButtonStyle}" Content="Apply filter" Command="{x:Bind ViewModel.ApplyCommand}" />
    </Grid>
  </Grid>
</Page>
