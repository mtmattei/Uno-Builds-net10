<Page x:Class="ChefsTest2.Presentation.RegisterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:utu="using:Uno.Toolkit.UI"
      Background="{ThemeResource SurfaceBrush}"
      NavigationCacheMode="Required">

  <Grid utu:SafeArea.Insets="VisibleBounds">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <utu:NavigationBar Content="Register" Grid.Row="0" />

    <ScrollViewer Grid.Row="1">
      <StackPanel Padding="32" Spacing="20" MaxWidth="500" HorizontalAlignment="Stretch">
        <TextBlock Style="{StaticResource ChefsTitleLarge}" Text="Create your account"
                   Foreground="{ThemeResource OnSurfaceBrush}" />
        <TextBox PlaceholderText="Username" Text="{x:Bind ViewModel.Username, Mode=TwoWay}" />
        <TextBox PlaceholderText="Email" Text="{x:Bind ViewModel.Email, Mode=TwoWay}" />
        <PasswordBox PlaceholderText="Password" Password="{x:Bind ViewModel.Password, Mode=TwoWay}" />

        <Button HorizontalAlignment="Stretch" Padding="0,16" Content="Sign Up"
                Background="{ThemeResource PrimaryBrush}"
                Foreground="{ThemeResource OnPrimaryBrush}"
                Command="{x:Bind ViewModel.SignUpCommand}"
                AutomationProperties.AutomationId="RegisterSignUp" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="4">
          <TextBlock Text="Already have an account?" Foreground="{ThemeResource OnSurfaceBrush}" />
          <Button Content="Login" Background="Transparent"
                  Foreground="{ThemeResource PrimaryBrush}"
                  Command="{x:Bind ViewModel.GoToLoginCommand}" />
        </StackPanel>
      </StackPanel>
    </ScrollViewer>
  </Grid>
</Page>
