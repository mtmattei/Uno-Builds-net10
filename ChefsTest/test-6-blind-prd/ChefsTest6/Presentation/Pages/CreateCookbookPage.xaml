<Page x:Class="ChefsTest6.Presentation.Pages.CreateCookbookPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:vm="using:ChefsTest6.ViewModels"
      x:Name="PageRoot"
      NavigationCacheMode="Required"
      Background="{ThemeResource BackgroundBrush}">
  <utu:SafeArea Insets="VisibleBounds,SoftInput">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <utu:NavigationBar Grid.Row="0" Content="Create cookbook" />

      <TextBox Grid.Row="1"
               Margin="16,8"
               Header="Cookbook name"
               Text="{Binding CookbookName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

      <ScrollViewer Grid.Row="2">
        <GridView ItemsSource="{Binding Picker}" SelectionMode="None" Margin="16,8">
          <GridView.ItemsPanel>
            <ItemsPanelTemplate>
              <ItemsWrapGrid Orientation="Horizontal" />
            </ItemsPanelTemplate>
          </GridView.ItemsPanel>
          <GridView.ItemTemplate>
            <DataTemplate x:DataType="vm:RecipePickerEntry">
              <Button Width="180" Margin="4" Background="Transparent" BorderThickness="0" Padding="0"
                      Command="{Binding DataContext.ToggleCommand, ElementName=PageRoot}"
                      CommandParameter="{x:Bind}">
                <Border CornerRadius="16" Background="{ThemeResource SurfaceVariantBrush}">
                  <Grid>
                    <StackPanel>
                      <Image Source="{x:Bind Recipe.ImageUrl}" Height="120" Stretch="UniformToFill" />
                      <StackPanel Padding="10" Spacing="2">
                        <TextBlock Text="{x:Bind Recipe.Name}" Style="{StaticResource TitleSmall}" TextWrapping="Wrap" />
                      </StackPanel>
                    </StackPanel>
                    <FontIcon HorizontalAlignment="Right" VerticalAlignment="Top"
                              Margin="8" FontSize="20"
                              Glyph="{x:Bind IsSelected, Mode=OneWay, Converter={StaticResource HeartGlyph}}"
                              Foreground="{ThemeResource PrimaryBrush}" />
                  </Grid>
                </Border>
              </Button>
            </DataTemplate>
          </GridView.ItemTemplate>
        </GridView>
      </ScrollViewer>

      <Grid Grid.Row="3" Padding="16" ColumnSpacing="12">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Content="Cancel" Style="{StaticResource OutlinedButtonStyle}" HorizontalAlignment="Stretch" Command="{Binding CancelCommand}" />
        <Button Grid.Column="1" Content="Create cookbook" Style="{StaticResource FilledButtonStyle}" HorizontalAlignment="Stretch" Command="{Binding SaveCommand}" />
      </Grid>
    </Grid>
  </utu:SafeArea>
</Page>
