<Page x:Class="ChefsTest7.Presentation.Pages.NotificationsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:models="using:ChefsTest7.Models"
      Background="{ThemeResource BackgroundBrush}">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <utu:NavigationBar Grid.Row="0" Content="Notifications"
                       Background="{ThemeResource SurfaceInverseBrush}"
                       Foreground="{ThemeResource OnSurfaceInverseBrush}">
      <utu:NavigationBar.PrimaryCommands>
        <AppBarButton Command="{x:Bind ViewModel.CloseCommand}">
          <AppBarButton.Icon><FontIcon Glyph="" /></AppBarButton.Icon>
        </AppBarButton>
      </utu:NavigationBar.PrimaryCommands>
    </utu:NavigationBar>
    <StackPanel Grid.Row="1" Orientation="Horizontal" Padding="16,8" Spacing="6">
      <Button Content="All" Style="{ThemeResource FilledButtonStyle}"/>
      <Button Content="Unread" Style="{ThemeResource OutlinedButtonStyle}"/>
      <Button Content="Read" Style="{ThemeResource OutlinedButtonStyle}"/>
    </StackPanel>
    <Grid Grid.Row="2">
      <ScrollViewer Visibility="{x:Bind ViewModel.IsEmpty, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
        <ItemsControl ItemsSource="{x:Bind ViewModel.All, Mode=OneWay}" Padding="16">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate><utu:AutoLayout Orientation="Vertical" Spacing="8"/></ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="models:Notification">
              <Border Background="{ThemeResource SurfaceVariantBrush}" CornerRadius="12" Padding="12">
                <StackPanel Spacing="4">
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*"/>
                      <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{x:Bind Title}" Style="{ThemeResource TitleSmall}" Foreground="{ThemeResource OnSurfaceBrush}"/>
                    <TextBlock Grid.Column="1" Text="{x:Bind Date, Converter={StaticResource RelativeDateConverter}}" Style="{ThemeResource BodySmall}" Foreground="{ThemeResource OnSurfaceVariantBrush}"/>
                  </Grid>
                  <TextBlock Text="{x:Bind Description}" Style="{ThemeResource BodyMedium}" Foreground="{ThemeResource OnSurfaceVariantBrush}" TextWrapping="Wrap"/>
                </StackPanel>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>
      <StackPanel Visibility="{x:Bind ViewModel.IsEmpty, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                  HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
        <TextBlock Text="🔔" FontSize="64" HorizontalAlignment="Center"/>
        <TextBlock Text="You're all caught up" Style="{ThemeResource TitleMedium}" Foreground="{ThemeResource OnSurfaceBrush}" HorizontalAlignment="Center"/>
        <TextBlock Text="No notifications right now" Style="{ThemeResource BodyMedium}" Foreground="{ThemeResource OnSurfaceVariantBrush}" HorizontalAlignment="Center"/>
      </StackPanel>
    </Grid>
  </Grid>
</Page>
