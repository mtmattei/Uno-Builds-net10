<Page x:Class="SmartCity.Presentation.ComingSoonPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:SmartCity.Presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="{ThemeResource SurfaceCanvasBrush}">

  <!-- MVUX page: DataContext is the generated bindable proxy, so {Binding} (not x:Bind). -->
  <Grid>
    <TextBlock Text="{Binding Message}"
               Style="{StaticResource TitleMedium}"
               Foreground="{ThemeResource MutedTextBrush}"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               TextAlignment="Center"
               MaxWidth="420"
               TextWrapping="Wrap" />
  </Grid>
</Page>
