<UserControl x:Class="FreewriteUno.InlineAi.Presentation.Controls.ActionPillView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:utu="using:Uno.Toolkit.UI"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

  <!-- Small capsule above the selection: two segments split by a hairline. DataContext is the InlineAi VM. -->
  <Border Background="{ThemeResource ChromeBackgroundBrush}"
          BorderBrush="{ThemeResource ChromeOutlineBrush}"
          BorderThickness="1"
          CornerRadius="22">
      <utu:AutoLayout Orientation="Horizontal"
                      CounterAxisAlignment="Stretch">
        <Button x:Name="AskButton"
                x:Uid="InlineAi_Pill_AskAi"
                Style="{StaticResource TextButtonStyle}"
                MinHeight="44"
                Padding="16,0"
                Background="Transparent"
                Command="{Binding OpenChat}"
                Click="OnSegmentClick"
                AutomationProperties.Name="Ask AI about the selection">
          <TextBlock Text="Ask AI" Style="{StaticResource AiChromeLabelStyle}" />
        </Button>

        <Rectangle Width="1"
                   Margin="0,8"
                   Fill="{ThemeResource ChromeOutlineBrush}" />

        <Button x:Name="ChatButton"
                x:Uid="InlineAi_Pill_Chat"
                Style="{StaticResource TextButtonStyle}"
                MinHeight="44"
                Padding="16,0"
                Background="Transparent"
                Command="{Binding OpenChat}"
                Click="OnSegmentClick"
                AutomationProperties.Name="Chat about the selection">
          <TextBlock Text="Chat" Style="{StaticResource AiChromeLabelStyle}" />
        </Button>
      </utu:AutoLayout>
  </Border>
</UserControl>
